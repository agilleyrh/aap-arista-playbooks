[defaults]
retry_files_enabled = False
host_key_checking = False
gathering = explicit
interpreter_python = auto_silent
# Inventory, credentials, and collections come from the AAP job template
# and its execution environment.

[inventory]
enable_plugins = auto, yaml, ini, constructed
